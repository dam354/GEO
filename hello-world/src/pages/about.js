import React from "react"
import Header from "../components/header"

export default () => (
  <div style={{ color: `teal` }}>
    <Header headerText="About Gatsby"  arbitrartPhrase="is arbitrary"/>
    <p>Such wow. Very React.</p>
  </div>
)